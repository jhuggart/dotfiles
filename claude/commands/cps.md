---
description: commit, push, and deploy branch to staging
---
1. Commit all staged changes with an appropriate message
2. Push to remote
3. Deploy the branch to staging using the staging workflow. Use @.github/workflows/staging.yml for context on how to trigger the deployment when available.
